from setuptools import setup

setup(name='dr_utils',
      version='0.1',
      description='Custom helpers for Disaster Reponse message classifier.',
      packages=['dr_utils'],
      author='Matthew Perkins',
      zip_safe=False)
